<div id="index-banner">
    <div class="section no-pad-bot">
        <div class="container">
            <div class="row" id="trips1">
                <div class="col s12">
                    {{#if trips.length}}
                        {{>trips trips=trips}}
                    {{else}}
                        <h5>Sorry, wanderluster, no trips found matching '{{searchQuery}}.'</h5>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>