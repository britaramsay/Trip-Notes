{{#if carousel}}
    {{#each trips}} 
        {{> publictrip trip=this}} 
    {{/each}}
{{/if}}
{{#unless carousel}}
    {{#each trips}} 
        {{> tripsummary trip=this}} 
    {{/each}}
{{/unless}}
