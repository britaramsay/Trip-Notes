{{#if carousel}}
    {{#each trips}} 
        {{> publictrip trip=this}} 
    {{/each}}
{{/if}}
{{owner}}
{{#unless carousel}}
    {{#each trips}} 
        {{> tripsummary trip=this owner=../owner}} 
    {{/each}}
{{/unless}}
